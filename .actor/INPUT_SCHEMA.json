{
    "title": "Greenhouse Job Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "urls": {
            "title": "Job Board URLs",
            "type": "array",
            "description": "Greenhouse job board URLs to scrape. To limit jobs per board: (1) Click 'Advanced' and add {\"maxJobs\": 20} to User data, OR (2) Switch to JSON tab and use {\"url\": \"...\", \"maxJobs\": 20}",
            "editor": "requestListSources",
            "placeholderKey": "url",
            "placeholderValue": "https://job-boards.greenhouse.io/",
            "patternKey": "url",
            "patternValue": "https://job-boards\\.greenhouse\\.io/.+",
            "prefill": [
                { 
                    "url": "https://job-boards.greenhouse.io/webflow"
                }
            ]
        },
        "proxy": {
            "title": "Proxy configuration",
            "type": "object",
            "description": "Proxy settings for the crawler",
            "editor": "proxy",
            "default": {
                "useApifyProxy": true
            }
        }
    },
    "required": ["urls"]
}
